package dev.originspaper.power.shared;

/**
 * Velvet Paws — silent footsteps. The public Paper API exposes no reliable way to suppress
 * sculk/step game-event vibrations without NMS, so this power is intentionally cosmetic and
 * exists for parity and display purposes only.
 */
public class SilentStepsPower extends AbstractPower {

    public SilentStepsPower(String id) {
        super(id);
    }
}
